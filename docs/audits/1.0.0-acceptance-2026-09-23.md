# 1.0.0 readiness acceptance / 发布验收

2026-09-23. This is an acceptance record, not a release announcement. The user authorized publication after all acceptance conditions; the candidate version is now 1.0.0.

| 项目 / Item | 结果 / Result | Evidence and boundary |
| --- | --- | --- |
| Desktop settings | Provider repair verified | Old official settings package fails import; selected release provider registers settings successfully. Desktop now binds official modules before launch and archives stale shadows. Third-party packages and profile configuration are retained. Local logs: `.tmp-ui-audit/desktop-binding-{core,agent,controller}.log`. |
| Session manager | Runtime attribution incomplete | Official Desktop Host with an isolated empty profile and installed session manager 0.5.1 returned HTTP 200 and valid JSON for annotations (39 bytes) and workspaces (60 bytes). Repeating with the archived incompatible settings provider reproduced its import failure but both APIs still returned 200. Actual Electron 44 protocol forwarding also passed. The historical screenshot failure was not reproduced; neither settings absence nor upstream Desktop fault is established. See session-response follow-up below. |
| Startup and timeout | Passed process fixtures | `.tmp-ui-audit/v1-startup-acceptance.log`: 3 real Node subprocess tests; one initial launch, bounded missing-dependency recovery, timeout diagnostic persistence without stopping the running process. Follow-up `.tmp-ui-audit/v1-timeout-followup.log` verifies that a timeout does not suppress later failure evidence in the same run. These are controlled fixtures, not all official Harness configurations. |
| Agent regression / version recovery | Passed automated checks | `.tmp-ui-audit/v1-agent-final.log`: 317 passed, 8 ignored. Includes stale profile switching, restore rollback, restart persistence and ownership guards. Ignored tests are not passes; 3 startup fixtures were separately executed above. |
| Offline relocation | Passed archive tests | `.tmp-ui-audit/v1-offline-acceptance.log`: 12 passed, including selected profiles and transitive plugin dependencies after relocation, credential preservation and space budgets. Does not claim every user profile has been imported on every OS. |
| Windows install and upgrade | Passed actual isolated NSIS run | `apps/nexus-launcher/electron-dist/update-e2e-oT5INN/report.json`, `.tmp-ui-audit/v1-update-e2e.log`. Dedicated test app identity; interrupted download, explicit consent, progress, deferred restart, install/restart, installed ASAR hash and retained preference verified. Test versions 0.1.3 to 0.1.4-local.1 do not represent a public Nexus release. |
| Packaged Windows application | Passed existing matching build checks | Build `20260923T080458684Z-c28620f4`: 14 executed release-gate checks, packaged and extracted ZIP resources plus actual Electron/Agent smoke. New timeout source changes require subsequent CI build; do not label this older package as containing them. |
| Git redistribution | Materials verified; final packaging pending | 213 notices and 62 corresponding-source files pass SHA-256 checks. Optional GCM excluded at the user’s request. Clearance covers only the processed Nexus distribution with retained notices and accompanying source assets. See [Git audit](git-redistribution-2026-09-23/README.md). |
| Five-platform CI | Pending new commit | Windows x64/ARM64, macOS x64/ARM64 and Linux ARM64 must run against the same current commit. Earlier 0.1.10 runs do not validate this change set. |

用户已手动验收；以上是独立证据，二者不互相替代。当前不能宣布 1.0.0 全部通过，不创建正式 tag，也不覆盖 D 盘安装。

The user has manually accepted the application. This record distinguishes independent automated, subprocess, installer and packaged-runtime evidence. No claim of full acceptance, public tag or D-drive deployment is made. Explicit runtime configuration still takes precedence over bundled tools; the proposed system-environment priority was cancelled.

## Session-response follow-up / 会话接口取证

Local private evidence: `.tmp-ui-audit/session-host-result.json`, `session-host-old-settings-result.json`, and `session-protocol-result.json`. Tests use a new temporary DSH home, official Desktop Host and the installed plugin; they do not read user sessions or change the production profile. Both API bodies contain valid empty results, not empty HTTP bodies. The Electron test separately runs the unchanged official forwarding function with real Chromium requests and an isolated loopback responder; it is not a full production Desktop UI test.

历史截图没有保存 HTTP 状态和响应体，不能从 JSON 解析异常还原其根因。当前隔离组合无法复现；需要在原问题再次出现时记录该次响应，不能把上述测试写成“原故障已修复”或“确认上游问题”。

## 1.0.0 candidate follow-up

- Full local release gate: all 14 executed checks passed, report target-rtest/release/verify-20260923T100122945Z-2e18e1f1/attempt-ZauZRd/verification.json. Build ID 20260923T100122945Z-2e18e1f1. Compatibility 36 passed, 1 skipped; offline migration/space 12 passed. Skips are not passes.
- Packaged resource hashes, runtimes, authenticated Agent/CLI status and actual isolated Electron/Agent startup passed: .tmp-ui-audit/release-100-resources.log and release-100-smoke.log. Actual offline repair through the packaged bridge passed without launching Harness.
- Packaged Windows Git has no matched GCM files and all 213 Git notices match their pinned hashes: release-100-git-package.log. Local 1.0.0 source companion extracted and all 62 source files matched their sizes and hashes: source-100-extracted-check.log. This local source artifact uses the pre-commit HEAD; final CI must regenerate exact final provenance.
- After that full gate, notice-only supplementation added valuable's exact original license and lazy-val's original MIT metadata plus clearly identified standard MIT wording. Generated inventory now has 418 components and zero unresolved scanned notice entries. This does not assert a blanket legal guarantee or that optional valuable was compiled. Final packaging is rerun for the changed notice payload; application behavior tests are reused because no application code changed.
- One manual packaging attempt omitted the existing NEXUS_UNSIGNED_SMOKE setting and correctly stopped at mandatory signing. The retry uses the same explicit unsigned acceptance mode as the gate and CI; no production signing rule was removed. Initial direct-node gate invocation was also stopped and superseded by the successful pnpm gate. Failed/aborted attempts are not passes.
- Final same-commit five-platform CI, six artifacts including corresponding source, public tag and 32 final attachments remain pending. D-drive installation and main remain unchanged.

Final notice payload follow-up: generated 418-component inventory has zero unresolved scanned notices; final ZIP resource/runtimes/Agent checks and actual Electron offline-repair acceptance passed (.tmp-ui-audit/release-100-final-zip-resources.log and release-100-final-zip-smoke.log). All 213 original Git notice hashes also match the staged Git blobs, with byte-preserving attributes. Prior commit 2e94e27 five-platform run 35843091162 succeeded; it does not validate the new 1.0.0 material changes, which require the next same-commit CI run.

Final NSIS and ZIP packaging completed successfully in the existing unsigned acceptance mode: .tmp-ui-audit/release-100-final-package-unsigned.log. Public signed/provenance release checks remain the separate CI publication step.
